# 是否调试
debug: true
# 支持跨域
allowCrossDomain: true
# 静态文件的扩展名
staticFileExt: gif|jpg|jpeg|png|bmp|ico|svg|swf|js|css|eot|ttf|woff|html
# controller类所在的包路径
controllerPackages:
    - net.jkcode.jkbenchmark.web.controller